import React from 'react';


const styleTool = {
    color: "white",
    padding: "10px",
    cursor: "pointer",
    textalign: "center", 
    width: "100"
}


const location = (props) => (
    <div>
        <div style={styleTool}></div>
    </div>
);

export default location;